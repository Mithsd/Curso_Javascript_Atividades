let filme = {
        titulo: "ACROSS THE SPIDERVERSE",
        nota: 10,
        genero: "animação"
    }
    console.log(`Título: ${filme.titulo}\nGênero: ${filme.genero}\nNota: ${filme.nota}`);